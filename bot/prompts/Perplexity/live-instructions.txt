RIGHT NOW it is {{NOW_UTC}}. You MUST search for the LIVE, REAL-TIME status of this exact match as of this exact moment.
STEP 1 - VERIFY MATCH STATUS FIRST: determine match_status as "not_started", "live", "halftime", or "finished". Even if not live/halftime, you must still fill score_a, score_b and minute with the most recent REAL values (e.g. final score and 90+X if finished, 0 and 0-0 if not started) - never null, never invented.
STEP 2 - LIVE SCORE AND MINUTE (HIGHEST PRIORITY): score_a and score_b MUST be the actual current goals scored by each team as integers, based on the latest live data (official FIFA feed, ESPN, Flashscore, SofaScore, BBC Sport, or equivalent). minute MUST be the actual current match minute (e.g. "67", "45+2", "HT", "FT"), not an estimate from kickoff time. Cross-check across at least two sources if possible; if they disagree use the most recently updated one. source_note must name which live source and what minute/timestamp it reported.
STEP 3 - KEY MATCH EVENTS: key_events must be a REAL array of objects { "minute", "type": "goal|red_card|yellow_card|penalty|var", "team", "player", "detail" } for events that have actually occurred. If none have happened yet, return an empty array []. Do NOT invent events.
STEP 4 - LIVE STATS: stats[] must be exactly three: 1) POSSESSION as "XX% - YY%", 2) SHOTS ON TARGET as "X - Y", 3) one more live stat (corners, fouls, or xG), all reflecting the CURRENT match state, not historical averages. If not started, use "0 - 0" and context "Match not started yet".
caption_hook MUST reflect the actual current score/minute/status - do not say "kickoff incoming" if the match is already underway.
